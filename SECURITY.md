# 安全政策

## 不可提交的数据

禁止提交 API Key、令牌、Cookie、聊天全文、客户代码、私有仓库路径、真实账单数据或包含敏感信息的原始路由日志。示例文件必须使用占位符。

## 发现泄露时

1. 立即撤销/轮换受影响凭据。
2. 停止相关路由，记录到 `logs/incident-log-template.md` 的副本中。
3. 评估提交历史、外部日志与缓存的暴露范围。
4. 删除并重写历史前先保留事件证据；公开仓库须假设泄露内容已被复制。

## 安全设计原则

执行模型使用最小权限；路由器默认拒绝不在白名单内的模型；任何不能证明模型身份、权限或测试结果的运行都不能进入下一阶段。
