# Never commit real credentials.
DEEPSEEK_API_KEY=
# Optional override; the official endpoint is used by default.
DEEPSEEK_BASE_URL=https://api.deepseek.com
# Set this when the Store-installed codex.exe cannot be spawned by your terminal.
CODEX_CLI_PATH=
