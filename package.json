{
  "name": "codex-model-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "route": "dist/src/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "check": "tsc -p tsconfig.json && vitest run",
    "route": "tsx src/cli.ts",
    "terminal": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/router-terminal.ps1"
  },
  "dependencies": { "commander": "^14.0.0" },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "engines": { "node": ">=22" }
}
