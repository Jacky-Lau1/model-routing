# sumPositive task

Fix `sumPositive(values)` in `src/sum.mjs`.

Contract:

- Sum only finite numbers greater than zero.
- Ignore zero, negative values, `NaN`, and infinities.
- Return `0` for an empty array or when no value qualifies.
- Do not mutate the input array.
- Only `src/sum.mjs` may be changed.
