# 贡献与维护约定

本仓库目前处于“设计归档”阶段。新增或修改方案前，先在 `logs/decision-log.md` 记录动机、证据、影响和回滚方式；涉及实现时还须同步更新路线图、验证记录和变更日志。

## 提交要求

- 不提交密钥或真实运行敏感数据。
- 策略变更必须同时修改 `config/routing-policy.example.yaml` 与对应文档。
- 任何“已验证”结论都要附可复现的测试范围、日期、版本和成功/失败数据。
- 任何成本结论都必须与同一任务集上的 Sol 基线比较。
- 不用模型自述来证明模型身份；以网关/服务端日志为准。

## 文档变更检查

1. 链接和文件路径正确；
2. 术语一致：规划者、执行者、质量门、升级、失败关闭；
3. 没有把待验证假设写成事实；
4. 更新 [CHANGELOG.md](CHANGELOG.md)；
5. 需要时提交对应 Issue，使用 `.github/ISSUE_TEMPLATE` 模板。
